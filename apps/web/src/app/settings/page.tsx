// ============================================================
// Settings Page — Route wrapper
// ============================================================

'use client';

import { SettingsPage } from '@/components/settings/SettingsPage';

export default function SettingsRoute() {
  return <SettingsPage />;
}
