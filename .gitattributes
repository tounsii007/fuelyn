# Force LF for backend Java sources so cross-platform Spotless stays
# deterministic (a Windows checkout previously committed CRLF, tripping
# the Linux spotless:check on CI). Applies in the repo AND the working tree.
*.java text eol=lf
